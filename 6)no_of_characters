'''
Create a program to take user input and out the number of times its character been repeated
'''


name = input("enter your name : ")

i = 0
temp = ""
while i < len(name):
    if name[i] not in temp:
        temp += name[i]
        print(f"{name[i]} : {name.count(name[i])}")
    i +=1
